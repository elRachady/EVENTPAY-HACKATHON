const express = require('express');
const router = express.Router();

// Définissez vos routes ici
router.get('/', (req, res) => {
  // Votre logique
});

// Exportez DIRECTEMENT le router
module.exports = router;